$('#banner').slick({
    autoplay:true,
    autoplaySpeed:1000,
    arrows:false
    // 箭頭去掉

});
$('#brand').slick({
    slidesToShow:3,
    autoplay:true,
    autoplaySpeed:800,
    dots:true,
    arrows:false

});